# SDK Split Implementation Plan (dart_sdk + flutter_sdk)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Split the ABSmartly SDK into a pure Dart SDK (`dart_sdk`) and a Flutter wrapper (`flutter_sdk`).

**Architecture:** Create a monorepo structure with two packages. The `dart_sdk` contains all core A/B testing logic with zero Flutter dependencies. The `flutter_sdk` depends on `dart_sdk` and adds Flutter-specific conveniences (widgets, providers, lifecycle integration). Both packages maintain backward compatibility with existing APIs.

**Tech Stack:** Dart 2.18+, Flutter 1.17+, package:collection for list/map equality

---

## Task 1: Create Package Directory Structure

**Files:**
- Create: `packages/dart_sdk/` directory
- Create: `packages/flutter_sdk/` directory

**Step 1: Create the package directories**

```bash
mkdir -p packages/dart_sdk/lib/src
mkdir -p packages/dart_sdk/test
mkdir -p packages/flutter_sdk/lib/src
mkdir -p packages/flutter_sdk/test
```

**Step 2: Verify directory structure**

Run: `find packages -type d | head -20`
Expected: Shows dart_sdk and flutter_sdk directories with lib/src and test subdirs

**Step 3: Commit**

```bash
git add packages/
git commit -m "chore: create package directory structure for sdk split"
```

---

## Task 2: Create dart_sdk pubspec.yaml

**Files:**
- Create: `packages/dart_sdk/pubspec.yaml`

**Step 1: Create pubspec.yaml**

```yaml
name: absmartly_dart
description: ABsmartly A/B Testing SDK for Dart - Pure Dart implementation with no Flutter dependencies
version: 2.1.2
homepage: https://github.com/absmartly/flutter-sdk
repository: https://github.com/absmartly/flutter-sdk/tree/main/packages/dart_sdk

environment:
  sdk: '>=2.18.6 <4.0.0'

dependencies:
  http_parser: '^4.0.2'
  async: '^2.9.0'
  crypto: '^3.0.6'
  http: '>=0.13.0 <2.0.0'
  intl: '>=0.19.0'
  collection: '^1.16.0'

dev_dependencies:
  test: '^1.24.0'
  mockito: '^5.4.2'
  build_runner: '^2.4.5'
  lints: '^3.0.0'
```

**Step 2: Verify pubspec is valid**

Run: `cd packages/dart_sdk && dart pub get`
Expected: Resolving dependencies... Got dependencies!

**Step 3: Commit**

```bash
git add packages/dart_sdk/pubspec.yaml
git commit -m "chore(dart_sdk): add pubspec.yaml with pure Dart dependencies"
```

---

## Task 3: Create dart_sdk analysis_options.yaml

**Files:**
- Create: `packages/dart_sdk/analysis_options.yaml`

**Step 1: Create analysis_options.yaml**

```yaml
include: package:lints/recommended.yaml

linter:
  rules:
    prefer_single_quotes: true
    sort_pub_dependencies: true
```

**Step 2: Commit**

```bash
git add packages/dart_sdk/analysis_options.yaml
git commit -m "chore(dart_sdk): add analysis_options.yaml"
```

---

## Task 4: Copy Core Source Files to dart_sdk

**Files:**
- Copy: All files from `lib/` to `packages/dart_sdk/lib/src/`

**Step 1: Copy all source files preserving directory structure**

```bash
cp -R lib/* packages/dart_sdk/lib/src/
```

**Step 2: Verify files were copied**

Run: `find packages/dart_sdk/lib -name "*.dart" | wc -l`
Expected: 67 (same as original)

**Step 3: Commit**

```bash
git add packages/dart_sdk/lib/
git commit -m "chore(dart_sdk): copy source files from lib/"
```

---

## Task 5: Fix Flutter Imports in context_data.dart

**Files:**
- Modify: `packages/dart_sdk/lib/src/json/context_data.dart`

**Step 1: Update imports and equality check**

Replace:
```dart
import 'package:flutter/foundation.dart';
```

With:
```dart
import 'package:collection/collection.dart';
```

Replace:
```dart
listEquals(other.experiments, experiments)
```

With:
```dart
const ListEquality().equals(other.experiments, experiments)
```

**Step 2: Verify file compiles**

Run: `cd packages/dart_sdk && dart analyze lib/src/json/context_data.dart`
Expected: No issues found!

**Step 3: Commit**

```bash
git add packages/dart_sdk/lib/src/json/context_data.dart
git commit -m "fix(dart_sdk): replace Flutter listEquals with collection package in context_data"
```

---

## Task 6: Fix Flutter Imports in experiment.dart

**Files:**
- Modify: `packages/dart_sdk/lib/src/json/experiment.dart`

**Step 1: Update imports**

Replace:
```dart
import 'package:flutter/foundation.dart';
```

With:
```dart
import 'package:collection/collection.dart';
```

**Step 2: Update all listEquals calls**

Replace all occurrences of:
```dart
listEquals(split, other.split)
listEquals(trafficSplit, other.trafficSplit)
listEquals(applications, other.applications)
listEquals(variants, other.variants)
```

With:
```dart
const ListEquality().equals(split, other.split)
const ListEquality().equals(trafficSplit, other.trafficSplit)
const ListEquality().equals(applications, other.applications)
const ListEquality().equals(variants, other.variants)
```

**Step 3: Verify file compiles**

Run: `cd packages/dart_sdk && dart analyze lib/src/json/experiment.dart`
Expected: No issues found!

**Step 4: Commit**

```bash
git add packages/dart_sdk/lib/src/json/experiment.dart
git commit -m "fix(dart_sdk): replace Flutter listEquals with collection package in experiment"
```

---

## Task 7: Fix Flutter Imports in goal_achievement.dart

**Files:**
- Modify: `packages/dart_sdk/lib/src/json/goal_achievement.dart`

**Step 1: Update imports**

Replace:
```dart
import 'package:flutter/foundation.dart';
```

With:
```dart
import 'package:collection/collection.dart';
```

**Step 2: Update mapEquals call**

Replace:
```dart
mapEquals(properties, other.properties)
```

With:
```dart
const MapEquality().equals(properties, other.properties)
```

**Step 3: Verify file compiles**

Run: `cd packages/dart_sdk && dart analyze lib/src/json/goal_achievement.dart`
Expected: No issues found!

**Step 4: Commit**

```bash
git add packages/dart_sdk/lib/src/json/goal_achievement.dart
git commit -m "fix(dart_sdk): replace Flutter mapEquals with collection package in goal_achievement"
```

---

## Task 8: Fix Flutter Imports in publish_event.dart

**Files:**
- Modify: `packages/dart_sdk/lib/src/json/publish_event.dart`

**Step 1: Update imports**

Replace:
```dart
import 'package:flutter/foundation.dart';
```

With:
```dart
import 'package:collection/collection.dart';
```

**Step 2: Update all listEquals calls**

Replace all occurrences of:
```dart
listEquals(other.units, units)
listEquals(other.exposures, exposures)
listEquals(other.goals, goals)
listEquals(other.attributes, attributes)
```

With:
```dart
const ListEquality().equals(other.units, units)
const ListEquality().equals(other.exposures, exposures)
const ListEquality().equals(other.goals, goals)
const ListEquality().equals(other.attributes, attributes)
```

**Step 3: Update the package import**

Replace:
```dart
import 'package:absmartly_sdk/json/unit.dart';
```

With:
```dart
import 'unit.dart';
```

**Step 4: Verify file compiles**

Run: `cd packages/dart_sdk && dart analyze lib/src/json/publish_event.dart`
Expected: No issues found!

**Step 5: Commit**

```bash
git add packages/dart_sdk/lib/src/json/publish_event.dart
git commit -m "fix(dart_sdk): replace Flutter listEquals with collection package in publish_event"
```

---

## Task 9: Update All Package Imports in dart_sdk

**Files:**
- Modify: All files in `packages/dart_sdk/lib/src/` that use `package:absmartly_sdk/`

**Step 1: Find all files with package imports**

```bash
grep -r "package:absmartly_sdk" packages/dart_sdk/lib/src/ --include="*.dart" -l
```

**Step 2: Update imports to relative paths**

For each file found, replace `package:absmartly_sdk/` imports with relative imports.

Example - in `packages/dart_sdk/lib/src/context.dart`:
Replace:
```dart
import 'package:absmartly_sdk/json/context_data.dart';
```
With:
```dart
import 'json/context_data.dart';
```

**Step 3: Verify all files compile**

Run: `cd packages/dart_sdk && dart analyze lib/`
Expected: No issues found!

**Step 4: Commit**

```bash
git add packages/dart_sdk/lib/
git commit -m "refactor(dart_sdk): convert package imports to relative imports"
```

---

## Task 10: Create dart_sdk Main Export File

**Files:**
- Create: `packages/dart_sdk/lib/absmartly_dart.dart`

**Step 1: Create the main export file**

```dart
library absmartly_dart;

export 'src/absmartly_sdk_config.dart';
export 'src/context.dart';
export 'src/client.dart';
export 'src/json/context_data.dart';
export 'src/context_event_handler.dart';
export 'src/context_data_provider.dart';
export 'src/default_context_data_provider.dart';
export 'src/default_context_event_handler.dart';
export 'src/json/attribute.dart';
export 'src/json/experiment.dart';
export 'src/json/experiment_variant.dart';
export 'src/json/exposure.dart';
export 'src/json/goal_achievement.dart';
export 'src/json/publish_event.dart';
export 'src/json/unit.dart';
```

**Step 2: Verify export file works**

Run: `cd packages/dart_sdk && dart analyze lib/absmartly_dart.dart`
Expected: No issues found!

**Step 3: Commit**

```bash
git add packages/dart_sdk/lib/absmartly_dart.dart
git commit -m "feat(dart_sdk): create main export file"
```

---

## Task 11: Copy and Update Tests for dart_sdk

**Files:**
- Copy: All files from `test/` to `packages/dart_sdk/test/`

**Step 1: Copy test files**

```bash
cp -R test/* packages/dart_sdk/test/
```

**Step 2: Update test imports from package:absmartly_sdk to package:absmartly_dart**

```bash
find packages/dart_sdk/test -name "*.dart" -exec sed -i '' 's/package:absmartly_sdk/package:absmartly_dart/g' {} \;
```

**Step 3: Update flutter_test to test package**

```bash
find packages/dart_sdk/test -name "*.dart" -exec sed -i '' 's/package:flutter_test\/flutter_test.dart/package:test\/test.dart/g' {} \;
```

**Step 4: Regenerate mocks (requires build_runner)**

```bash
cd packages/dart_sdk && dart run build_runner build --delete-conflicting-outputs
```

**Step 5: Run tests**

Run: `cd packages/dart_sdk && dart test`
Expected: All tests pass

**Step 6: Commit**

```bash
git add packages/dart_sdk/test/
git commit -m "test(dart_sdk): copy and update tests for pure Dart"
```

---

## Task 12: Create dart_sdk README.md

**Files:**
- Create: `packages/dart_sdk/README.md`

**Step 1: Create README.md**

```markdown
# ABsmartly Dart SDK

Pure Dart SDK for [ABsmartly](https://www.absmartly.com/) A/B testing platform. This package has no Flutter dependencies and can be used in any Dart environment (CLI, server-side, Flutter).

## Compatibility

The ABsmartly Dart SDK is compatible with Dart versions 2.18.6 and later.

## Getting Started

### Install the SDK

Add to your `pubspec.yaml`:

```yaml
dependencies:
  absmartly_dart: ^2.1.2
```

### Import and Initialize the SDK

```dart
import 'package:absmartly_dart/absmartly_dart.dart';

void main() async {
  final ClientConfig clientConfig = ClientConfig()
    ..setEndpoint("https://your-company.absmartly.io/v1")
    ..setAPIKey("YOUR-API-KEY")
    ..setApplication("website")
    ..setEnvironment("development");

  final ABSmartlyConfig sdkConfig = ABSmartlyConfig.create()
      .setClient(Client.create(clientConfig));
  final ABSmartly sdk = ABSmartly(sdkConfig);
}
```

**SDK Options**

| Config      | Type                              | Required? |   Default   | Description                                                                                                                                                                   |
| :---------- | :-------------------------------- | :-------: | :---------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| endpoint    | `String`                          |  &#9989;  | `undefined` | The URL to your API endpoint. Most commonly `"your-company.absmartly.io"`                                                                                                     |
| apiKey      | `String`                          |  &#9989;  | `undefined` | Your API key which can be found on the Web Console.                                                                                                                           |
| environment | `"production"` or `"development"` |  &#9989;  | `undefined` | The environment of the platform where the SDK is installed. Environments are created on the Web Console and should match the available environments in your infrastructure.   |
| application | `String`                          |  &#9989;  | `undefined` | The name of the application where the SDK is installed. Applications are created on the Web Console and should match the applications where your experiments will be running. |

## Create a New Context Request

### Synchronously

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context? context = sdk.createContext(contextConfig).waitUntilReady();

if (context != null) {
  print("context ready");
}
```

### Asynchronously

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context? context = await sdk.createContext(contextConfig).waitUntilReady();

if (context != null) {
  print("context ready");
}
```

### With Prefetched Data

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context context = sdk.createContext(contextConfig).waitUntilReady();

final ContextConfig anotherContextConfig = ContextConfig.create()
    .setUnit("session_id", "another-user-id");

final Context anotherContext = sdk.createContextWith(anotherContextConfig, context.getData());
assert(anotherContext.isReady()); // no need to wait
```

### Refreshing the Context with Fresh Experiment Data

For long-running contexts, use `setRefreshInterval()` to automatically refresh experiment data:

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8")
    .setRefreshInterval(TimeUnit.HOURS.toMillis(4)); // every 4 hours
```

Or call `refresh()` manually:

```dart
context.refresh();
```

### Setting Extra Units

```dart
context.setUnit("db_user_id", "1000013");
context.setUnits({
  "db_user_id": "1000013"
});
```

> **Note:** You cannot override an already set unit type. Create a new context instead.

## Basic Usage

### Selecting a Treatment

```dart
if (await context.getTreatment("exp_test_experiment") == 0) {
  // user is in control group (variant 0)
} else {
  // user is in treatment group
}
```

### Treatment Variables

```dart
var defaultButtonColorValue = "red";
var buttonColor = context.getVariableValue("button.color", defaultButtonColorValue);
```

### Peek at Treatment Variants

Check treatment without triggering an exposure:

```dart
var value = await context.peekTreatment("experimentName");
```

### Overriding Treatment Variants

```dart
context.setOverride("exp_test_experiment", 1);
context.setOverrides({
  "exp_test_experiment": 1,
  "exp_another_experiment": 0,
});
```

## Advanced

### Context Attributes

```dart
context.setAttribute("user_agent", "Mozilla/5.0...");
context.setAttributes({
  "customer_age": "new_customer",
});
```

### Custom Assignments

```dart
context.setCustomAssignment("experimentName", 1);
context.setCustomAssignments({"experimentName": 1});
```

### Tracking Goals

```dart
context.track("payment", {
  "item_count": 1,
  "total_amount": 1999.99
});
```

### Publish

Ensure all events are published:

```dart
await context.publish();
```

### Finalize

Close the context and publish pending events:

```dart
await context.close();
```

### Custom Event Logger

```dart
class CustomEventLogger implements ContextEventLogger {
  @override
  void handleEvent(Context context, EventType event, dynamic data) {
    switch (event) {
      case EventType.Exposure:
        final Exposure exposure = data;
        print("exposed to experiment ${exposure.name}");
        break;
      case EventType.Goal:
        final GoalAchievement goal = data;
        print("goal tracked: ${goal.name}");
        break;
      case EventType.Error:
        print("error: $data");
        break;
      case EventType.Publish:
      case EventType.Ready:
      case EventType.Refresh:
      case EventType.Close:
        break;
    }
  }
}

// Usage
contextConfig.setContextEventLogger(CustomEventLogger());
```

**Event Types**

| Event      | When                                               | Data                                   |
| ---------- | -------------------------------------------------- | -------------------------------------- |
| `Error`    | Context receives an error                          | Error object                           |
| `Ready`    | Context turns ready                                | ContextData used to initialize         |
| `Refresh`  | `refresh()` method succeeds                        | ContextData used to refresh            |
| `Publish`  | `publish()` method succeeds                        | PublishEvent sent to collector         |
| `Exposure` | `getTreatment()` succeeds on first exposure        | Exposure enqueued for publishing       |
| `Goal`     | `track()` method succeeds                          | GoalAchievement enqueued for publishing|
| `Close`    | `close()` method succeeds the first time           | null                                   |

## Platform Support

| Platform    | Support |
|-------------|---------|
| Dart VM     | ✅ |
| Flutter     | ✅ (use `absmartly_sdk` for Flutter-specific features) |
| Web         | ✅ |
| Server-side | ✅ |

## Flutter Users

For Flutter applications, use the [`absmartly_sdk`](https://pub.dev/packages/absmartly_sdk) package which wraps this SDK and may include Flutter-specific features in the future.

## Documentation

- [Full Documentation](https://docs.absmartly.com/)
- [API Reference](https://pub.dev/documentation/absmartly_dart/latest/)

## License

MIT License - see [LICENSE](../../LICENSE) for details.
```

**Step 2: Commit**

```bash
git add packages/dart_sdk/README.md
git commit -m "docs(dart_sdk): add comprehensive README.md"
```

---

## Task 13: Create flutter_sdk pubspec.yaml

**Files:**
- Create: `packages/flutter_sdk/pubspec.yaml`

**Step 1: Create pubspec.yaml**

```yaml
name: absmartly_sdk
description: ABsmartly A/B Testing SDK for Flutter - Flutter wrapper with widgets and providers
version: 2.1.2
homepage: https://github.com/absmartly/flutter-sdk
repository: https://github.com/absmartly/flutter-sdk/tree/main/packages/flutter_sdk

environment:
  sdk: '>=2.18.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  absmartly_dart:
    path: ../dart_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '>=3.0.0 <6.0.0'

flutter:
```

**Step 2: Verify pubspec is valid**

Run: `cd packages/flutter_sdk && flutter pub get`
Expected: Got dependencies!

**Step 3: Commit**

```bash
git add packages/flutter_sdk/pubspec.yaml
git commit -m "chore(flutter_sdk): add pubspec.yaml with absmartly_dart dependency"
```

---

## Task 14: Create flutter_sdk Main Export File

**Files:**
- Create: `packages/flutter_sdk/lib/absmartly_sdk.dart`

**Step 1: Create the main export file**

```dart
library absmartly_sdk;

// Re-export the entire Dart SDK
export 'package:absmartly_dart/absmartly_dart.dart';

// Flutter-specific exports (to be added later)
// export 'src/absmartly_provider.dart';
// export 'src/experiment_builder.dart';
```

**Step 2: Verify export file works**

Run: `cd packages/flutter_sdk && flutter analyze lib/absmartly_sdk.dart`
Expected: No issues found!

**Step 3: Commit**

```bash
git add packages/flutter_sdk/lib/absmartly_sdk.dart
git commit -m "feat(flutter_sdk): create main export file re-exporting absmartly_dart"
```

---

## Task 15: Create flutter_sdk analysis_options.yaml

**Files:**
- Create: `packages/flutter_sdk/analysis_options.yaml`

**Step 1: Create analysis_options.yaml**

```yaml
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
    sort_pub_dependencies: true
```

**Step 2: Commit**

```bash
git add packages/flutter_sdk/analysis_options.yaml
git commit -m "chore(flutter_sdk): add analysis_options.yaml"
```

---

## Task 16: Move Example App to flutter_sdk

**Files:**
- Move: `example/` to `packages/flutter_sdk/example/`

**Step 1: Move example directory**

```bash
mv example packages/flutter_sdk/
```

**Step 2: Update example pubspec.yaml dependency**

In `packages/flutter_sdk/example/pubspec.yaml`, update the SDK dependency:

```yaml
dependencies:
  flutter:
    sdk: flutter
  absmartly_sdk:
    path: ../
```

**Step 3: Verify example compiles**

Run: `cd packages/flutter_sdk/example && flutter pub get && flutter analyze`
Expected: No issues found!

**Step 4: Commit**

```bash
git add -A
git commit -m "chore(flutter_sdk): move example app to flutter_sdk package"
```

---

## Task 17: Create flutter_sdk README.md

**Files:**
- Create: `packages/flutter_sdk/README.md`

**Step 1: Create README.md**

```markdown
# ABsmartly Flutter SDK

Flutter SDK for [ABsmartly](https://www.absmartly.com/) A/B testing platform. This package wraps the pure Dart SDK and provides seamless Flutter integration.

## Compatibility

The ABsmartly Flutter SDK is compatible with:
- Dart versions 2.18.6 and later
- Flutter 1.17.0 and later

## Getting Started

### Install the SDK

Add to your `pubspec.yaml`:

```yaml
dependencies:
  absmartly_sdk: ^2.1.2
```

### Import and Initialize the SDK

```dart
import 'package:absmartly_sdk/absmartly_sdk.dart';

void main() async {
  final ClientConfig clientConfig = ClientConfig()
    ..setEndpoint("https://your-company.absmartly.io/v1")
    ..setAPIKey("YOUR-API-KEY")
    ..setApplication("website")
    ..setEnvironment("development");

  final ABSmartlyConfig sdkConfig = ABSmartlyConfig.create()
      .setClient(Client.create(clientConfig));
  final ABSmartly sdk = ABSmartly(sdkConfig);

  runApp(MyApp(sdk: sdk));
}
```

**SDK Options**

| Config      | Type                              | Required? |   Default   | Description                                                                                                                                                                   |
| :---------- | :-------------------------------- | :-------: | :---------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| endpoint    | `String`                          |  &#9989;  | `undefined` | The URL to your API endpoint. Most commonly `"your-company.absmartly.io"`                                                                                                     |
| apiKey      | `String`                          |  &#9989;  | `undefined` | Your API key which can be found on the Web Console.                                                                                                                           |
| environment | `"production"` or `"development"` |  &#9989;  | `undefined` | The environment of the platform where the SDK is installed. Environments are created on the Web Console and should match the available environments in your infrastructure.   |
| application | `String`                          |  &#9989;  | `undefined` | The name of the application where the SDK is installed. Applications are created on the Web Console and should match the applications where your experiments will be running. |

## Create a New Context Request

### Synchronously

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context? context = sdk.createContext(contextConfig).waitUntilReady();

if (context != null) {
  print("context ready");
}
```

### Asynchronously

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context? context = await sdk.createContext(contextConfig).waitUntilReady();

if (context != null) {
  print("context ready");
}
```

### With Prefetched Data

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8");

final Context context = sdk.createContext(contextConfig).waitUntilReady();

final ContextConfig anotherContextConfig = ContextConfig.create()
    .setUnit("session_id", "another-user-id");

final Context anotherContext = sdk.createContextWith(anotherContextConfig, context.getData());
assert(anotherContext.isReady()); // no need to wait
```

### Refreshing the Context with Fresh Experiment Data

For long-running contexts, use `setRefreshInterval()` to automatically refresh experiment data:

```dart
final ContextConfig contextConfig = ContextConfig.create()
    .setUnit("session_id", "5ebf06d8cb5d8137290c4abb64155584fbdb64d8")
    .setRefreshInterval(TimeUnit.HOURS.toMillis(4)); // every 4 hours
```

Or call `refresh()` manually:

```dart
context.refresh();
```

### Setting Extra Units

```dart
context.setUnit("db_user_id", "1000013");
context.setUnits({
  "db_user_id": "1000013"
});
```

> **Note:** You cannot override an already set unit type. Create a new context instead.

## Basic Usage

### Selecting a Treatment

```dart
if (await context.getTreatment("exp_test_experiment") == 0) {
  // user is in control group (variant 0)
} else {
  // user is in treatment group
}
```

### Treatment Variables

```dart
var defaultButtonColorValue = "red";
var buttonColor = context.getVariableValue("button.color", defaultButtonColorValue);
```

### Peek at Treatment Variants

Check treatment without triggering an exposure:

```dart
var value = await context.peekTreatment("experimentName");
```

### Overriding Treatment Variants

```dart
context.setOverride("exp_test_experiment", 1);
context.setOverrides({
  "exp_test_experiment": 1,
  "exp_another_experiment": 0,
});
```

## Advanced

### Context Attributes

```dart
context.setAttribute("user_agent", "Mozilla/5.0...");
context.setAttributes({
  "customer_age": "new_customer",
});
```

### Custom Assignments

```dart
context.setCustomAssignment("experimentName", 1);
context.setCustomAssignments({"experimentName": 1});
```

### Tracking Goals

```dart
context.track("payment", {
  "item_count": 1,
  "total_amount": 1999.99
});
```

### Publish

Ensure all events are published:

```dart
await context.publish();
```

### Finalize

Close the context and publish pending events:

```dart
await context.close();
```

### Custom Event Logger

```dart
class CustomEventLogger implements ContextEventLogger {
  @override
  void handleEvent(Context context, EventType event, dynamic data) {
    switch (event) {
      case EventType.Exposure:
        final Exposure exposure = data;
        print("exposed to experiment ${exposure.name}");
        break;
      case EventType.Goal:
        final GoalAchievement goal = data;
        print("goal tracked: ${goal.name}");
        break;
      case EventType.Error:
        print("error: $data");
        break;
      case EventType.Publish:
      case EventType.Ready:
      case EventType.Refresh:
      case EventType.Close:
        break;
    }
  }
}

// Usage
contextConfig.setContextEventLogger(CustomEventLogger());
```

**Event Types**

| Event      | When                                               | Data                                   |
| ---------- | -------------------------------------------------- | -------------------------------------- |
| `Error`    | Context receives an error                          | Error object                           |
| `Ready`    | Context turns ready                                | ContextData used to initialize         |
| `Refresh`  | `refresh()` method succeeds                        | ContextData used to refresh            |
| `Publish`  | `publish()` method succeeds                        | PublishEvent sent to collector         |
| `Exposure` | `getTreatment()` succeeds on first exposure        | Exposure enqueued for publishing       |
| `Goal`     | `track()` method succeeds                          | GoalAchievement enqueued for publishing|
| `Close`    | `close()` method succeeds the first time           | null                                   |

## Flutter Integration

This package is designed for Flutter applications and includes all functionality from the [Dart SDK](../dart_sdk/).

### Example App

See the [example](example/) directory for a complete Flutter application demonstrating the SDK:

```bash
cd example
flutter run
```

### Future Flutter-Specific Features

We're planning to add Flutter-specific features such as:
- `ABSmartlyProvider` - InheritedWidget for context propagation
- `ExperimentBuilder` - Widget that rebuilds on treatment changes
- App lifecycle integration (pause/resume event publishing)

## Dart-Only Projects

For non-Flutter Dart projects (CLI tools, server-side), use the [`absmartly_dart`](https://pub.dev/packages/absmartly_dart) package directly for a smaller dependency footprint.

## Documentation

- [Full Documentation](https://docs.absmartly.com/)
- [API Reference](https://pub.dev/documentation/absmartly_sdk/latest/)

## License

MIT License - see [LICENSE](../../LICENSE) for details.
```

**Step 2: Commit**

```bash
git add packages/flutter_sdk/README.md
git commit -m "docs(flutter_sdk): add comprehensive README.md"
```

---

## Task 18: Update Root pubspec.yaml (Optional Workspace)

**Files:**
- Modify or remove: root `pubspec.yaml`

**Step 1: Option A - Keep root pubspec for local development**

Update root `pubspec.yaml` to be a workspace reference:

```yaml
name: absmartly_sdk_workspace
description: ABsmartly SDK Monorepo
version: 0.0.0
publish_to: none

environment:
  sdk: '>=2.18.6 <4.0.0'
```

**Step 2: Option B - Remove root pubspec**

If you prefer packages to be fully independent:

```bash
rm pubspec.yaml pubspec.lock
```

**Step 3: Commit**

```bash
git add -A
git commit -m "chore: update root pubspec for monorepo structure"
```

---

## Task 19: Update Root README.md

**Files:**
- Modify: `README.md`

**Step 1: Update the root README to document the monorepo structure**

Add a section at the top:

```markdown
# ABsmartly SDK

This repository contains the official ABsmartly SDKs for Dart and Flutter.

## Packages

| Package | Description | Pub |
|---------|-------------|-----|
| [dart_sdk](packages/dart_sdk/) | Pure Dart SDK - works in any Dart environment | [![pub](https://img.shields.io/pub/v/absmartly_dart.svg)](https://pub.dev/packages/absmartly_dart) |
| [flutter_sdk](packages/flutter_sdk/) | Flutter SDK - includes Flutter-specific features | [![pub](https://img.shields.io/pub/v/absmartly_sdk.svg)](https://pub.dev/packages/absmartly_sdk) |

## Which Package Should I Use?

- **Flutter apps**: Use `absmartly_sdk` (the Flutter package)
- **Dart CLI/Server**: Use `absmartly_dart` (the pure Dart package)
- **Package authors**: Depend on `absmartly_dart` for maximum compatibility

## Development

```bash
# Get dependencies for dart_sdk
cd packages/dart_sdk && dart pub get

# Get dependencies for flutter_sdk
cd packages/flutter_sdk && flutter pub get

# Run dart_sdk tests
cd packages/dart_sdk && dart test

# Run flutter_sdk tests
cd packages/flutter_sdk && flutter test
```

---

(Keep existing README content below for reference)
```

**Step 2: Commit**

```bash
git add README.md
git commit -m "docs: update root README for monorepo structure"
```

---

## Task 20: Clean Up Old Files

**Files:**
- Remove: `lib/` directory (now in packages/dart_sdk)
- Remove: `test/` directory (now in packages/dart_sdk)

**Step 1: Remove old directories**

```bash
rm -rf lib test
```

**Step 2: Verify package structure**

```bash
find packages -type f -name "*.dart" | wc -l
```

Expected: All Dart files now in packages/

**Step 3: Commit**

```bash
git add -A
git commit -m "chore: remove old lib/ and test/ directories"
```

---

## Task 21: Run Full Test Suite

**Files:** None (verification only)

**Step 1: Run dart_sdk tests**

```bash
cd packages/dart_sdk && dart test
```

Expected: All tests pass

**Step 2: Run flutter_sdk analysis**

```bash
cd packages/flutter_sdk && flutter analyze
```

Expected: No issues found

**Step 3: Build flutter_sdk example**

```bash
cd packages/flutter_sdk/example && flutter build apk --debug
```

Expected: Build succeeds

**Step 4: Final commit**

```bash
git add -A
git commit -m "chore: verify all packages build and test successfully"
```

---

## Summary

After completing all tasks, the repository structure will be:

```
absmartly-sdk/
├── packages/
│   ├── dart_sdk/                    # Pure Dart SDK
│   │   ├── lib/
│   │   │   ├── absmartly_dart.dart  # Main export
│   │   │   └── src/                 # All source code
│   │   ├── test/                    # Tests
│   │   ├── pubspec.yaml
│   │   ├── analysis_options.yaml
│   │   └── README.md
│   │
│   └── flutter_sdk/                 # Flutter wrapper
│       ├── lib/
│       │   └── absmartly_sdk.dart   # Re-exports dart_sdk
│       ├── example/                 # Flutter example app
│       ├── pubspec.yaml
│       ├── analysis_options.yaml
│       └── README.md
│
├── README.md                        # Monorepo documentation
├── LICENSE
└── .github/                         # CI/CD workflows
```

### Key Changes

1. **dart_sdk** is now a standalone pure Dart package
2. **flutter_sdk** depends on dart_sdk and re-exports it
3. All Flutter-specific code removed from core SDK (4 files updated)
4. Tests work with pure Dart test runner
5. Example app moved to flutter_sdk package

### Future Enhancements (flutter_sdk)

After the split, consider adding Flutter-specific features:
- `ABSmartlyProvider` - InheritedWidget for context propagation
- `ExperimentBuilder` - Widget builder for variant rendering
- `useExperiment()` - Hook-style API if using flutter_hooks
- App lifecycle integration (pause/resume publishing)
